const cardsReducer = (state, action) => {
    switch(action.type) {
        case 'createNewCards':
            return {...state, }
        case 'deleteCards':
            return {...state, }
        case 'correctCards':
            return {...state, }
        default:
            return {...state}
    }
}

export default bundlesCards;